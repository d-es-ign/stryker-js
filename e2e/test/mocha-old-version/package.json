{
  "name": "mocha-old-version",
  "version": "0.0.0",
  "private": true,
  "description": "A test package for mocha 5",
  "main": "index.js",
  "scripts": {
    "clean:reports": "rimraf \"reports\"",
    "verify": "mocha --no-config --no-package --timeout 0 verify/verify.js",
    "test:unit": "mocha",
    "test": "npm run clean:reports && stryker run && npm run verify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "7.2",
    "@d-es-ign/stryker-js-core": "workspace:*",
    "@d-es-ign/stryker-js-mocha-runner": "workspace:*"
  },
  "dependenciesMeta": {
    "@d-es-ign/stryker-js-core": {
      "injected": true
    },
    "@d-es-ign/stryker-js-mocha-runner": {
      "injected": true
    }
  }
}

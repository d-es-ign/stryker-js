{
  "name": "vitest-old-version",
  "version": "0.0.0",
  "private": true,
  "description": "The e2e test for the oldest supported vitest version",
  "main": "index.js",
  "scripts": {
    "clean:reports": "rimraf \"reports\"",
    "verify": "mocha --no-config --no-package --timeout 0 verify/verify.js",
    "test:unit": "vitest run",
    "test": "npm run clean:reports && stryker run && npm run verify"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vitest": "^2.0.0",
    "@d-es-ign/stryker-js-core": "workspace:*",
    "@d-es-ign/stryker-js-vitest-runner": "workspace:*"
  },
  "dependenciesMeta": {
    "@d-es-ign/stryker-js-core": {
      "injected": true
    },
    "@d-es-ign/stryker-js-vitest-runner": {
      "injected": true
    }
  }
}
